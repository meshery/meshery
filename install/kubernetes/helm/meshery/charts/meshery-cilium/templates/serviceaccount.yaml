{{- if and (not .Values.serviceAccountNameOverride) .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "meshery-cilium.serviceAccountName" . }}
  namespace: {{ .Values.metadata.namespace }}
  labels:
{{- include "meshery-cilium.labels" . | nindent 4 }}
{{- end -}}
