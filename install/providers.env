# Canonical list of Meshery's remote providers - the single source of truth for
# provider URLs across every install artifact (docker-compose files, the Helm chart,
# raw k8s manifests, the playground manifests, the docker-extension chooser, the UI
# deep-link constants, the e2e defaults, the mesheryctl fallback, and the server's
# viper default). Each line is `Name=URL`: Name is the human-friendly provider name
# (also a valid PROVIDER pre-selection value and the label shown in the extension
# chooser), URL is the provider's base URL.
#
# After editing, run `make providers-propagate` to propagate to all generated artifacts.
# `make providers-check` (CI) fails if anything is out of sync.
#
# ACTIVE providers are registered at server startup and
# emitted into PROVIDER_BASE_URLS. They MUST resolve in DNS: the server retries each
# unreachable provider URL 2x (3s apart) at startup, so an unreachable host adds ~6s
# before Meshery serves traffic and breaks the e2e/meshsync integration tests. The
# first active provider is the PRIMARY (used by single-URL consumers: the docker
# extension, playground, UI deep-links, provider-ui return-to, and e2e defaults).
"Meshery"=https://cloud.meshery.io
"Intel"=https://perf.smp-spec.io
"Layer5"=https://cloud.layer5.io
"TATA Labs"=https://platform.tata-consulting.co.uk
"Cisco ET&I"=https://collab.eti.cisco.com
"Metabit Trading"=https://kickstart.metabit.com
"OD10 Ventures"=https://provider.od10.in
#
# DECLARED providers in the lines below are part of the
# canonical roster but do not desire for their remote providers to be included
# in the standard Meshery release packaging. Each of these remote providers
# separately enable their provider their own environments.
# "DigitalOcean"=https://meshery.digitalocean.com
# "CleverLuck"=https://idp.cleverluck.com
# "Ginger Geek"=https://designs.gingergeek.com
# "HPE Labs"=https://ppe.eng.hpe.com
# "Asteria Aerospace"=https://maps.asteria.co.in
# "Exoscale"=https://designer.exoscale.com
# "UT Austin"=https://ppf.research.utexas.edu
# "HPE Security Engineering"=https://meshguard.eng.hpe.com
# "Intraserve Systems"=https://plated.intraserve.com
# "SolarWinds"=https://eng-eu-02.solarwinds.com
# "Metabob"=https://overt.metabob.com
# "BootLabs"=https://m.bootlabs.com
# "AYA Innovation Labs"=https://spar-cse.aya-innovation.com
# "F5"=https://ec2-54-123-45-27.us-west-1.compute.amazonaws.com
