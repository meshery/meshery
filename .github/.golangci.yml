version: "2"
run:
  timeout: 5m

linters:
  disable:
    - errcheck
  exclusions:
    paths:
      - server/handlers/doc.go
      - server/models/meshmodel/core/register.go

linters-settings:
  staticcheck:
    checks:
      - "all"
      - "-ST1005"
