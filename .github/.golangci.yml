version: "2"
run:
  timeout: 5m

linters:
  disable:
    - errcheck
  settings:
    staticcheck:
      checks: ["all", "-ST1005"]
  exclusions:
    paths:
      - server/handlers/doc.go
      - server/models/meshmodel/core/register.go
