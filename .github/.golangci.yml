version: "2"
run:
  timeout: 5m

linters:
  exclusions:
    rules:
      - linters:
          - staticcheck
        text: "ST1005:"
    paths:
      - server/handlers/doc.go
      - server/models/meshmodel/core/register.go
