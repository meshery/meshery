version: "2"
run:
  timeout: 5m

linters:
  settings:
    staticcheck:
      checks: 
        - ST1005
        - QF1004
  disable:
    - errcheck
  exclusions:
    paths:
      - server/handlers/doc.go
      - server/models/meshmodel/core/register.go
 
