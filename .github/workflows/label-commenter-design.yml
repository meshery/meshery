name: Design Label Commenter

on:
  issues:
    types:
      - labeled

permissions:
  contents: read
  issues: write

jobs:
  comment:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main # Set your default branch

      - name: Label Commenter
        uses: peaceiris/actions-label-commenter@v1