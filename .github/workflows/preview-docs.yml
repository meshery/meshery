name: Deploy Docs Preview
on:
  workflow_run:
    workflows:
      - Build and Preview Docs
    types:
      - completed

jobs:
  deploy-docs-preview:
    if: github.repository == 'meshery/meshery' && github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@master

      - name: Download Site directory
        uses: dawidd6/action-download-artifact@v6
        with:
          github_token: ${{ secrets.GH_ACCESS_TOKEN }}
          workflow: build-and-preview-docs.yml
          run_id: ${{ github.event.workflow_run.id }}
          name: docs-site

      - name: Unzip Site
        run: |
          mkdir -p docs-preview
          unzip docs-site.zip -d docs-preview
          rm -f docs-site.zip

      - name: Deploy to Netlify
        id: netlify
        uses: nwtgck/actions-netlify@v3
        with:
          publish-dir: 'docs-preview'
          production-deploy: false
          github-token: ${{ secrets.GH_ACCESS_TOKEN }}
          enable-commit-comment: false
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - name: Comment Deploy URL
        uses: ./.github/actions/comment-preview-on-pr
        with:
          token: ${{ secrets.GH_ACCESS_TOKEN }}
          deploy_url: "${{ steps.netlify.outputs.deploy-url }}"
