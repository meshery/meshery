import { describe, it } from 'vitest';

// PrometheusSelectionComponent renders a paginated picker for prometheus
// boards. It is bound to RTK Query for board metadata, a redux slice
// for the selected boards, and the GrafanaCustomCharts preview panel.
describe.skip('PrometheusSelectionComponent', () => {
  it('skipped - RTK Query backed board picker with redux state', () => {});
});
