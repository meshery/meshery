import { describe, it } from 'vitest';

// PrometheusComponent is the top-level prometheus telemetry view; it
// composes PrometheusConfigComponent and PrometheusSelectionComponent
// (each individually skipped above) plus a chart preview panel. There
// is no leaf behaviour here apart from prop-forwarding.
describe.skip('PrometheusComponent', () => {
  it('skipped - composes Config + Selection prometheus children', () => {});
});
