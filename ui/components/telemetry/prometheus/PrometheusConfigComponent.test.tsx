import { describe, it } from 'vitest';

// PrometheusConfigComponent is the connection form for a Prometheus
// instance, backed by RTK Query, redux dispatch, and several sistent
// primitives. The schema definition for the credentials is unit-tested
// in components/schemas/credentials/prometheus.test.tsx.
describe.skip('PrometheusConfigComponent', () => {
  it('skipped - RTK Query connection form with redux + sistent UI', () => {});
});
