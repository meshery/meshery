import { describe, it } from 'vitest';

// GrafanaSelectionComponent renders a board-picker that fetches grafana
// dashboards via RTK Query and maintains the selected boards in redux.
describe.skip('GrafanaSelectionComponent', () => {
  it('skipped - RTK Query backed board picker with redux state', () => {});
});
