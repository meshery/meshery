import { describe, it } from 'vitest';

// GrafanaCustomGaugeChart is a billboard.js gauge variant of
// GrafanaCustomChart, with the same polling and timer dependencies.
describe.skip('GrafanaCustomGaugeChart', () => {
  it('skipped - billboard.js gauge chart with fetch lifecycle', () => {});
});
