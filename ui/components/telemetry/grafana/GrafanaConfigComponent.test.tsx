import { describe, it } from 'vitest';

// GrafanaConfigComponent is the connection form for a Grafana instance,
// backed by RTK Query (useSaveGrafanaConfigMutation), redux dispatch and
// sistent UI primitives. The credential schema is tested in
// components/schemas/credentials/grafana.test.tsx.
describe.skip('GrafanaConfigComponent', () => {
  it('skipped - RTK Query backed connection form', () => {});
});
