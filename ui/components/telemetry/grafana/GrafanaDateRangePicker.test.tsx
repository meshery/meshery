import { describe, it } from 'vitest';

// GrafanaDateRangePicker renders a complex dialog with many sistent UI
// primitives (Dialog, DialogContentText, DateTimePicker, Switch, etc.)
// plus a custom date-time picker. The range-token to Date mapping it
// drives the parent component with is unit-tested through
// helper.test.tsx (grafanaDateRangeToDate).
describe.skip('GrafanaDateRangePicker', () => {
  it('skipped - heavy dialog with DateTimePicker + sistent surface', () => {});
});
