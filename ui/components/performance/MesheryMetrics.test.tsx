import { describe, it } from 'vitest';

// MesheryMetrics is a high-level dashboard for grafana / prometheus
// connection chips + selection forms. It uses redux selectors for
// telemetry connection state, RTK Query for telemetry config, and a
// number of sistent UI primitives. The selection logic is owned by
// GrafanaSelectionComponent and PrometheusSelectionComponent which
// have their own redux/RTK plumbing.
describe.skip('MesheryMetrics', () => {
  it('skipped - dashboard wiring multiple telemetry RTK Query slices', () => {});
});
