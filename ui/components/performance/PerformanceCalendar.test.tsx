import { describe, it } from 'vitest';

// PerformanceCalendar renders a react-big-calendar bound to per-day test
// run counts; it computes events from a RTK Query result and projects
// them on a styled Calendar component. The visual surface is heavy (date
// selection, drill-down popovers) and exercised via playwright.
describe.skip('PerformanceCalendar', () => {
  it('skipped - react-big-calendar driven view of RTK Query test runs', () => {});
});
