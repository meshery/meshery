/**
 * User keys API - uses @meshery/schemas generated RTK hooks.
 * Re-exported here so consumers can import from @/rtk-query/userKeys.
 */
export { useGetUserKeysQuery } from '@meshery/schemas/dist/mesheryApi';
