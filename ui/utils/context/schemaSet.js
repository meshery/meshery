import { createContext } from "react";

export const SchemaContext = createContext({ workloadTraitSet : null, meshWorkloads : null })
// values are initialised in `ui/components/ConfiguratorWrapper.js`
