const opacity = 0.5;

export const disabledStyle = {
  pointerEvents: 'none',
  opacity,
};

export const disabledStyleWithOutOpacity = {
  pointerEvents: 'none',
};

export const cursorNotAllowed = {
  cursor: 'not-allowed',
};
