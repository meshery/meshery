contexts:
  local:
    endpoint: http://localhost:9081
    token: Default
    platform: docker
    components:
      - meshery-istio
    channel: stable
    version: v0.6.6
current-context: local
tokens:
  - location: auth.json
    name: Default
