contexts:
    local:
        endpoint: http://localhost:9081
        token: default
        platform: kubernetes
        components:
            - meshery-istio
        channel: stable
        version: latest
    local2:
        endpoint: http://localhost:32242
        token: default2
        platform: docker
        components:
            - meshery-istio
        channel: stable
        version: latest
current-context: local
tokens:
    - location: auth.json
      name: default
    - location: auth.json
      name: default2
