contexts:
    gke:
        channel: stable
        components:
            - meshery-istio
        endpoint: http://localhost:9081
        platform: gke
        token: default
        version: latest
    local:
        channel: stable
        components:
            - meshery-istio
        endpoint: http://localhost:9081
        platform: docker
        token: default
        version: latest
    local2:
        endpoint: http://localhost:9081
        token: default
        platform: docker
        components:
            - meshery-istio
        channel: edge
        version: latest
current-context: local
tokens:
    - location: auth.json
      name: default
