contexts:
    gke:
        channel: stable
        components:
            - meshery-istio
        endpoint: http://localhost:9081
        platform: gke
        token: default
        version: latest
    local:
        channel: stable
        components:
            - meshery-istio
        endpoint: http://localhost:9081
        platform: docker
        token: default
        version: latest
    local2:
        channel: stable
        components:
            - meshery-istio
        endpoint: http://localhost:9081
        platform: docker
        token: default
        version: latest
current-context: local2
tokens:
    - location: auth.json
      name: default
